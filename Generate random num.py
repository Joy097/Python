import random

   #a=int(input('Give the amount of numbers you want to print :'))
   #x=int(input('Give the lowest number in range :'))
   #y=int(input('Give the highest number in range :'))

for i in range(10):    #use a for input numbers amount
    print(random.randint(-50,99),',',end='')   #use range (x,y)
    #print('\n')
